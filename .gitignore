node_modules
.next
dist
.builds
.env
.env.local
*.log
.turbo
.DS_Store
*.tsbuildinfo
.mcp.json
# secrets — never commit (RSA delegation keypair, PEMs, key dumps)
private.txt
public.txt
*.pem
*.key

# stray local screenshots / camera exports — keep them out of `git add .`
/.neon
/*.jpeg
/*.png
/apps/*.png
**/Screenshot*.png

# local demo/test inputs (camera exports, CSV/PNG fed to the builder) — not source
/sample-data/
